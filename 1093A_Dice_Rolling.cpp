#include <bits/stdc++.h>

#define boost1 ios::sync_with_stdio(0)
#define boost2 cin.tie(0)

using namespace std;

int main()
{
    boost1; boost2;
    int n;
    cin>>n;
    for(int i = 0; i < n; i++) {
        int a;
        cin>>a;
        cout<<(a/2)<<endl;
    }
    return 0;
}
